export default function InterestsLayout({ children }) {
    return (
      <div style={{ fontFamily: 'Courier New, monospace', color: 'green', padding: '20px' }}>
        {children}
      </div>
    );
  }