export default function AboutLayout({ children }) {
    return (
      <div style={{ fontFamily: 'Arial, sans-serif', color: 'blue', padding: '20px' }}>
        {children}
      </div>
    );
  }