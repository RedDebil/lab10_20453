export default function FavoriteGameLayout({ children }) {
    return (
      <div style={{ fontFamily: 'Georgia, serif', color: 'darkred', padding: '20px' }}>
        {children}
      </div>
    );
  }